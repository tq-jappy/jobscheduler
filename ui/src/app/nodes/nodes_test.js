'use strict';

describe('ui.nodes module', function() {

  beforeEach(module('ui.nodes'));

  describe('node controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var nodeCtrl = $controller('NodeCtrl');
      expect(nodeCtrl).toBeDefined();
    }));

  });
});